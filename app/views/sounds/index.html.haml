%h1 Listing sounds
%table
  %thead
    %tr
      %th Name
      %th Author
      %th Location
      %th{:colspan => "3"}
  %tbody
    - @sounds.each do |sound|
      %tr
        %td= sound.name
        %td= sound.author
        %td= sound.location_id
        %td= link_to 'Show', sound
        %td= link_to 'Edit', edit_sound_path(sound)
        %td= link_to 'Destroy', sound, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Sound', new_sound_path
